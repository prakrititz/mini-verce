# orbit

Frontend for Mini-Vercel.

## Local development

1. Open this folder.
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the dev server:
   ```bash
   npm run dev
   ```

The frontend expects the backend daemon to be available separately from the `back-end/` folder.
